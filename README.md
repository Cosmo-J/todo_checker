# Usage
the executable requires a single argument which is the path to the directory containing .c files which will be scanned for substrings of `//todo`

# Example
`./todo_finder .` will run the program in the current directory.
